// Import config
import { auth } from "./firebaseConfig";

// Export configuration
export const Colors = {
  primary: "#6bff",
  secondary: "#FF4500",
  black: "#000000",
  white: "#ffffff",
  mediumGray: "#B0B0B0",
};

export const Images = {
  defaultLogo: require("../assets/images/TimelessLogo.png"),
};

// Export Firebase auth
export { auth } from "./firebaseConfig";

