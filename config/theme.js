// App theme configuration
export const Colors = {
  // Primary and secondary colors
  primary: "#f57c00", // Main accent color (orange)
  secondary: "#039be5", // Secondary color (blue)

  // Neutral colors
  black: "#222222", // Text and dark backgrounds
  white: "#ffffff", // Default background or text
  mediumGray: "#6e6869", // Placeholder text or secondary elements

  // Error and alert colors
  error: "#fc5c65", // Error messages or destructive actions
};

export const Theme = {
  colors: Colors,
  // Add other theme properties here (e.g., fonts, spacing, etc.)
};
