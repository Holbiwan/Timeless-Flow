// Centralized export for custom hooks
// Add all hooks here for easy imports across the app

import { useTogglePasswordVisibility } from './useTogglePasswordVisibility';

export {
  useTogglePasswordVisibility, // Hook for toggling password visibility
  // Add new hooks here as needed
};
